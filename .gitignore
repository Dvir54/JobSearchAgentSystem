# Secrets
.env

# Personal data — never push the real resume
base_cv.txt
base_cv.md

# The per-user Canva profile, written by `jobs init` — names one person's design.
# Anchored to the repo root: an unanchored pattern also swallowed the test
# fixture, so the suite passed on a file that existed on one machine only.
/profile.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editable-install build metadata, regenerated by `pip install -e .`
*.egg-info/
build/
dist/

# Agent output
output/

# Per-day run logs, written by `jobs run` (see cli.tee_stderr_to_log)
logs/
